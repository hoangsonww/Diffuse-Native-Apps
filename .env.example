# Diffuse does not read environment files at runtime. This file exists so
# clones that copy `.env.example` → `.env` do not invent secrets.
#
# Signing identities, team IDs, and notarization credentials live outside
# this repository. Do not put them here.
