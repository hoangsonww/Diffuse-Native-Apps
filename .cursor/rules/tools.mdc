---
description: diffuse-dev CLI — thin client of the domain engine
globs: Tools/**
alwaysApply: false
---

Read `Tools/AGENTS.md` and `Documentation/CLI.md`. Commands call `SnapshotCoding` / `DiffEngine` / `ReportRenderer` / `SnapshotValidator`. Do not reimplement diff or storage in the CLI.
